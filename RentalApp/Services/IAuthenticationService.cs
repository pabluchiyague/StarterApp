// Moved to RentalApp.Database/Services/IAuthenticationService.cs so the
// service is reachable from RentalApp.Tests (which targets net9.0 and
// can't reference the MAUI project). Namespace is unchanged
// (RentalApp.Services), so all consumers compile without modification.
