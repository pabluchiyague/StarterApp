<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="RentalApp.Views.NearbyItemsPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:RentalApp.Database.Models;assembly=RentalApp.Database"
             Title="{Binding Title}">
    <Grid RowDefinitions="Auto,*" Padding="16" RowSpacing="12">
        <VerticalStackLayout Spacing="10">
            <Label Text="{Binding ErrorMessage}"
                   TextColor="Red"
                   IsVisible="{Binding HasError}" />
            <Label Text="{Binding SearchSummary}" />
            <Label Text="{Binding RadiusKm, StringFormat='Radius: {0:0.#}km'}" />
            <Slider Minimum="1"
                    Maximum="50"
                    Value="{Binding RadiusKm}" />
            <Button Text="Find near me"
                    Command="{Binding FindNearMeCommand}" />
        </VerticalStackLayout>

        <Grid Grid.Row="1">
            <CollectionView ItemsSource="{Binding Items}">
                <CollectionView.EmptyView>
                    <Label Text="No nearby items found."
                           HorizontalOptions="Center"
                           VerticalOptions="Center" />
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Item">
                        <Border StrokeThickness="1"
                                Stroke="#DDDDDD"
                                Padding="12"
                                Margin="0,0,0,10">
                            <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="*,Auto">
                                <Label Text="{Binding Title}"
                                       FontSize="18"
                                       FontAttributes="Bold" />
                                <Label Grid.Column="1"
                                       Text="{Binding DailyRate, StringFormat='£{0:0.00}/day'}"
                                       FontAttributes="Bold" />
                                <Label Grid.Row="1"
                                       Grid.ColumnSpan="2"
                                       Text="{Binding DistanceKm, StringFormat='{0:0.0}km away'}" />
                                <Button Grid.Row="2"
                                        Grid.Column="1"
                                        Text="View"
                                        Command="{Binding BindingContext.OpenItemCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}"
                                        CommandParameter="{Binding .}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               HorizontalOptions="Center"
                               VerticalOptions="Center" />
        </Grid>
    </Grid>
</ContentPage>
